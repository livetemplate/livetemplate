# BroadcastAction Redesign — Live Progress Tracker

**Canonical plan:** the §"Phased implementation plan (tracker)" section of `docs/proposals/broadcast-action-redesign-proposal.md` (the converged Publish/Subscribe spec, #415; read-only baseline).
**This file:** the writable companion. Each implementation session (one phase at a time — typically a Claude Code session, per the multi-session model this loop is built for) updates it as its **first and last action**. The proposal section never mutates; all execution drift lives here.

---

## Phase Status

| Phase | Status | Owner / session | Started | Completed | Learnings file | Notes |
|---|---|---|---|---|---|---|
| 0 — Foundations (registry + topics.go) | complete | claude-code / 2026-05-17 | 2026-05-17 | 2026-05-17 | `phase-0.md` ✔ | Additive only; gate (non-race + lint) green. ⚠ Phase 1 must resolve the flagged subscribe-after-Unregister race (see `phase-0.md`). |
| 1 — Context API + ACL (single instance) | complete | claude-code / 2026-05-18 | 2026-05-18 | 2026-05-18 | `phase-1.md` ✔ | Gate GREEN (V1–V7,V10–V12,V16,V19,V21; non-race suite + enforced lint 0 issues + scoped `-race`). 5 deviations (3 audit-predicted + `msg.GroupID`-absent + typed `*TopicForbiddenError`); nil-`r` ACL hardening shipped. `BroadcastAction` untouched. **Not committed — awaiting user signoff.** Phase 2 seam: `dispatchToTopic` has no remote leg yet. |
| 2 — Cross-instance (Redis) | complete | claude-code / 2026-05-18 | 2026-05-18 | 2026-05-18 | `phase-2.md` ✔ | Gate GREEN (V8, V9 via 2 RedisBroadcasters on 1 testcontainer; full `scripts/pre-commit.sh` incl. Redis + lint 0 issues + scoped `-race`; Phase-1 V-items regression-green). 6 deviations (registry returns transition-bool + `SubscribedTopics`; root-pkg LIFO disconnect-sweep; `LiveHandler.Publish` not `*Template`; 2 real broadcasters not in-memory; seen-ring → Phase 3; round-4 normalization landed). Benchmarks: fan-out N=1..100 = 0.9µs..99µs; cross-instance 0.96× the GroupActionMessage baseline (no overhead). Inherited #420/#422 blocker was already resolved upstream by #421/#423. `BroadcastAction` untouched. **Not committed — awaiting user signoff.** Phase 3 seam: `b.pubsub` single-PubSub for PSUBSCRIBE; `subscribedPatterns` parallel map; seen-ring folds into `handleTopicActionMessage`. |
| 3 — Wildcards (multi-segment) | complete | claude-code / 2026-05-19 | 2026-05-19 | 2026-05-19 | `phase-3.md` ✔ | Gate GREEN (V17, V18 — single-instance deterministic + cross-instance Redis testcontainers; full `scripts/pre-commit.sh` incl. Redis + lint **0 issues** + scoped `-race` pubsub/internal-session/topic; cross-instance 3×-repeat no flake; Phase-2 V-items regression-green). **Multi-segment wildcards HOLD in v1** (explicit Phase-3 call — no reduction; general `segmentMatch` carried straight to PSUBSCRIBE). 4 deviations (seen-ring in pubsub-layer `handleTopicActionMessage` per the canonical "inside the processMessages pump" text, not mount.go — advisor-confirmed; strict re-match already structural in `GetByTopicExcept`+`segmentMatch`; `subKind` param single-sources the #420/#422 race loop, group-action byte-unchanged; relay branch centralized as `*liveHandler` methods, fixed a latent pattern-mis-release bug in `releaseRelayedTopics`). Bench item 2: pattern-scan O(P) ≈220ns/pattern (P=1..100 = 0.84µs..22µs) — flat scan adequate, no trie (as designed). `BroadcastAction` untouched. **Not committed — awaiting user signoff.** Phase 4 seam: no Phase-3 client-envelope change. Phase 5 seam: removal surface unchanged (subKind only *tags* BroadcastAction's subscribes `subExact`). --- Audit (see `phase-3.md`): local pattern machinery (`byTopicPattern`/`isPatternTopic`/`GetByTopicExcept`+`segmentMatch`) already Phase-0-built ⇒ no registry change; strict re-match already structural in `GetByTopicExcept` (confirm+test only). Surfaced: `Publish` to a `*` pattern currently panics `GetByTopicExcept` — Phase 3 adds a reject guard. Seen-ring placed in **pubsub-layer** `handleTopicActionMessage` (proposal §"Cross-instance exactly-once" = "inside the processMessages pump"; before the mount `GetByTopicExcept`) — recorded deviation vs phase-2.md's literal site. Inherits from Phase 2 (see `phase-2.md` "Adjustments"): PSUBSCRIBE on the **same `b.pubsub`** the single `processMessages` pump reads; add `subscribedPatterns map[string]int` ∥ `subscribedChannels` (replay in `reconnect()`); relay branches on `isPatternTopic` (exact-SUBSCRIBE vs PSUBSCRIBE — never expand a pattern); **seen-ring `(instanceID,seq)` dedup lands here** (N=64 bounded, single pump ⇒ no lock) folded into `handleTopicActionMessage` before `GetByTopicExcept` — **two ring constraints (doc'd at the `seq` field, from #424 round-3 review): `seq` monotonic per-instance not per-Type; `seq==0` = pre-upgrade sender ⇒ ring MUST bypass dedup (process unconditionally, no double-fire from pre-Phase-2 instances)**; local strict `segmentMatch` re-match for PSUBSCRIBE over-delivery also folds there; `releaseRelayedTopics`/transition-bool pattern generalizes to patterns as-is. |
| 4 — Client error envelope (parallelizable 1–3) | complete | claude-code / 2026-05-19 | 2026-05-19 | 2026-05-19 | `phase-4.md` ✔ | **V14 GREEN on all three tiers** — Tier 1 livetemplate Go integration (`TestTopic_V14_MountDenyEmitsEnvelopeAndKeepsConnectionOpen`, 0.00s), client jest logic leg (4/4 V14 tests; full client suite 29 suites/551 tests pass), Tier 2 lvt chromedp browser e2e (`TestE2E_V14_TopicACLDeniedEmitsLvtErrorAndKeepsWSOpen`, 1.57s end-to-end with full 4-artifact capture). Full `scripts/pre-commit.sh` green in livetemplate (fmt + golangci-lint **0 issues** + full Redis-testcontainer suite); lvt non-browser regression green; scoped `-race` clean. **Audit RESHAPED the proposal's TS-only Phase 4 into a 3-repo coordinated change (recorded as Deviation 1):** (a) **livetemplate** — Option B keep-open server change in `mount.go` (for `*TopicForbiddenError` send envelope + Warn + fall through to the shared success-path lifecycle adopting the controller's returned newState; non-TopicForbidden Mount errors keep byte-identical close behavior); stale `topic_runtime.go` comment updated; new Tier-1 regression in `topic_test.go`. (b) **client** — first-discriminator `type==="error"` branch in `handleWebSocketPayload` dispatches `lvt:error` CustomEvent `{code,topic}` on wrapper (non-bubbling, mirrors `lvt:updated` idiom); name overlaps with form-lifecycle `lvt:error` (different target/detail, non-bubbling, pinned per spec — Phase 6 docs); jest V14 logic leg with target-specific + over-match-guard assertions. (c) **lvt** — V14 Tier-2 chromedp e2e with full 4-artifact capture (console / server logs via slog.SetDefault save+restore / WS frames via RecordWSFrames / OuterHTML); `IsInitialMount` guard on the controller (Deviation 3: Mount runs on both HTTP GET + WS connect, denied Subscribe on GET 500s — pre-existing per phase-1.md); local Phase-4 client bundle served (not `ServeClientLibrary` — Deviation 4: CDN+1h disk cache shadows `LVT_CLIENT_CDN_URL`); **committed go.mod replace** `github.com/livetemplate/livetemplate => ../../../livetemplate/.worktrees/broadcast-redesign-phase-4` (Deviation 2 — Phase-5-resolved cross-repo dependency artifact; lvt Phase-4 branch intentionally not independently mergeable until Phase 5's pin bump, matching proposal's "lvt e2e gates last" release order). 8 surfaced-scope items rolled into the Ledger. **Server envelope ↔ lvt:error byte-for-byte agreement pinned** (`{"type":"error","code":"topic_forbidden","topic":"private/admin"}` ↔ `detail:{code:"topic_forbidden",topic:"private/admin"}` on wrapper). `BroadcastAction` untouched (still through Phase 4 — Phase 5 removes). **Not committed — three branches local in `<repo>/.worktrees/broadcast-redesign-phase-4`, awaiting user signoff.** Phase 5 seams: convert the lvt go.mod replace to a real version pin; revert lvt e2e's local-bundle handler to `e2etest.ServeClientLibrary` post client npm publish; fix lvt `scripts/pre-commit.sh` `--disable-all` v2-incompatibility (pre-existing on lvt main line 36, lint silently broken, 84+ pre-existing issues — out of Phase 4 scope). Phase 6 seams: 5 docs deliverables — `lvt:error` contract; distinguish from form `lvt:error`; keep-open behavior; controller-swallows-error case; `IsInitialMount` guard pattern; HTTP-GET-denied still 500. |
| 5 — Removal + in-repo/lvt migration | complete (livetemplate side) | claude-code / 2026-05-20 | 2026-05-20 | 2026-05-20 | `phase-5.md` ✔ | **livetemplate-side complete — atomic single commit removes `ctx.BroadcastAction` + dead group-dispatch (`processBroadcasts`/`dispatchBroadcastToGroup`) + 5 migrated call sites + comment-ref rewording across 10+ files; substring-grep zero-match across `BroadcastAction`/`broadcastRequest`/`pendingBroadcasts`/`MaxBroadcastsPerAction`/`processBroadcasts`/`dispatchBroadcastToGroup`.** Three audit-was-wrong deviations (see `phase-5.md` "Deviations"): (i) **`PublishGroupAction` kept** — `session_impl.go:180` is a live caller (`Session.TriggerAction` cross-instance). (ii) **`MaxBroadcastsPerAction` renamed `MaxPublishesPerAction` + relocated to `topic_context.go`** — Phase 4's Publish reuses the same cap. Note: exported constant rename is itself a v0.10.0 breaking-change line. (iii) **`topic_phase2_coverage_test.go` was missed by the audit** (6 refs all updated). Word-boundary regex blind spot recorded (`\b…\b` doesn't match `Test…_…` because `_` is `\w` — final sweep used substring form). lvt Phase-5 PR follows after this livetemplate PR merges + tags v0.10.0. **Atomic commit `2703d1d0` opened as livetemplate#429** (2026-05-20, off `main` @ `ab855777` v0.9.2). --- Audit context: `BroadcastAction` wire/semantics/dispatch path untouched by Phase 3 **and Phase 4** (Phase 4 added the keep-open `mount.go` change on the topic Subscribe/ACL surface — orthogonal to the `BroadcastAction` removal). The `grep -rn '\.BroadcastAction(' . --include='*.go'` sweep is unaffected (Phases 3–4 added no BroadcastAction call sites). New `pubsub` symbols (`subscribedPatterns`, `seenRing`, `TopicPatternSubscriber`, `SubscribeToTopicPattern`/`UnsubscribeFromTopicPattern`) are topic-only — none touch BroadcastAction; listed so the removal-surface audit's topic-vs-group-action classification is complete. `pubsub/redis_test.go` now references `subExact` (5 internal call sites). **Phase 4 adds new Phase-5 work (from `phase-4.md` adjustments):** (a) **resolve the committed lvt go.mod `replace` into a real version pin** — the deliverable's `go.mod pin bump` step is literally this conversion (anchor: `replace github.com/livetemplate/livetemplate => ../../../livetemplate/.worktrees/broadcast-redesign-phase-4`); (b) **revert lvt V14 e2e's `serveLocalPhase4ClientBundle` to `e2etest.ServeClientLibrary`** once `@livetemplate/client` publishes with the Phase-4 `lvt:error` branch; (c) **lvt non-browser regression is already green** against the Phase-4 livetemplate worktree (Phases 0–4 backward-compatible with current lvt code; verified this session) — Phase 5's removals are the first time livetemplate-side changes will require lvt-side migration; (d) **optionally fix lvt `scripts/pre-commit.sh` `--disable-all` v2-incompatibility** (one-line: `--disable-all` → `--default=none`; lint silently broken since flag removal — 84+ pre-existing issues — out of Phase 4 scope; bringing the gate back will surface them at once). |
| 6 — Docs + examples + ecosystem | not started | — | — | — | `phase-6.md` | From #424 round-4 review (see `phase-2.md` Open questions): docs/troubleshooting must surface the operator contract that a logged-only `SubscribeToTopicActions` init failure at `Handle()` ⇒ the instance's entire cross-instance topic-receive leg is dead (consistent w/ pre-existing GroupAction/ServerAction init pattern; not a code change). Also: out-of-band `handler.Publish` emits no symmetry-collision warning (trusted-code rationale). **Phase 4 adds new Phase-6 docs deliverables (from `phase-4.md` adjustments):** (1) **`lvt:error` CustomEvent contract** on `[data-lvt-id]` wrapper with `detail:{code,topic}`, non-bubbling; byte-for-byte agreement with the server envelope (`{"type":"error","code":"topic_forbidden","topic":…}`). (2) **Distinguish from the form-level `lvt:error`** in `state/form-lifecycle-manager.ts` (different target = `<form>`, different detail shape = `ResponseMetadata`); two distinct events sharing the name by historical accident — neither bubbles so they never collide at a listener, but `grep lvt:error` will look like a bug otherwise. (3) **Keep-open behavior** on a denied Subscribe in WS-connect Mount: the `lvt:error` fires, the WS stays open and functional, no auto-reconnect storm. Explicit contrast with the pre-Phase-4 close behavior. (4) **Controller-swallows-error case**: `_ = ctx.Subscribe("denied"); return s, nil` → **no envelope emitted**. V14's contract is on the propagated-error scenario only. (5) **`IsInitialMount` guard pattern** for controllers subscribing topics the ACL may deny — to avoid the pre-existing HTTP-GET-denied-→-500 aborting the page before the WS can exercise keep-open. (6) **HTTP-GET denied behavior remains HTTP 500** (unchanged from Phase 1; surface in troubleshooting). (7) **Cross-repo release order pattern**: the committed-`replace`-resolved-by-pin-bump shape (Phase 4's lvt go.mod) is the canonical pattern for future cross-repo dev waves that introduce APIs on the dependee before the dependency publishes. (8) **Partial-state adoption on keep-open** (surfaced by livetemplate#427 round-1 bot review): if a controller mutates `s` before the denied Subscribe and then `return s, err`, that partially-modified state is silently adopted (not rolled back) by Option B's fall-through. Document alongside the controller-swallows-error case (item 4): "to surface envelope, propagate the error; to keep state clean, don't mutate `s` before a Subscribe that may be denied." |

Status values: `not started` → `in progress` → `blocked` → `complete`.

---

## How to use this file

**At the start of your phase:**

1. Read the §"Phased implementation plan (tracker)" + §"Per-phase audit + learnings protocol" sections of `docs/proposals/broadcast-action-redesign-proposal.md`.
2. Read the previous phase's `phase-N-1.md` (Phase 0 has none; Phase 4 reads only `phase-0.md`; Phase 6 reads all). It carries the **adjustments recommended for the next phase** — apply them before doing the phase's own Audit.
3. Run this phase's **Audit (start)** as specified in the proposal. The Audit MAY reshape the task list — that is expected, not a deviation; record what changed.
4. Mark this phase's row `in progress` with your session id + date.
5. Create `phase-N.md` from the template at the bottom of this file.

**At the end of your phase:**

1. Fill `phase-N.md` completely — do not skip "New scope surfaced" even if the answer is "none."
2. Roll every surfaced scope item into the Ledger below.
3. Update the Phase Status row: status → `complete`, fill the completed date + learnings-file check.
4. If you discovered work that changes a later phase's scope, note it in that phase's row so the next session sees it before reading the plan.
5. Do not create mid-session WIP commits without an explicit request. `phase-N.md` is **not** ephemeral: it is committed as that phase's close-out record, in the phase's own implementation PR alongside its code, and stays in git history (it is the durable handoff the next phase's Audit reads). "Leave uncommitted" applies only to in-session drafts before the phase closes.

**If you block:** set status `blocked`, record the blocker in the row, write `phase-N-partial.md` with what was attempted and why it stalled. Once unblocked, set status back to `in progress`, clear/annotate the blocker note, and record the resumption date in the `Notes` column.

---

## Surfaced-Scope & Deferral Ledger

The analog of a running budget: every phase rolls (a) scope surfaced mid-phase and (b) anything that maps onto an Appendix B "Alternatives considered" entry into one visible place. There is **no `Deferred (post-v1)` section** in this spec, and the proposal (including Appendix B) is the read-only baseline — so an item is never "written into Appendix B." It either ships in v1 or is **logged here as reconciled-with-Appendix-B**: Appendix B's text stays frozen; the journal entry below IS the record. Phase 6's exit performs the final pass.

### Known at design time (pre-seeded from the converged spec)

- **Severe footgun — dispatch-symmetry naming hazard.** `Publish(topic, "Delete", …)` invokes the same `Delete` a client-wired `name="Delete"` triggers, on every subscribed peer. v1 hard requirement: `Publish` MUST `slog.Warn` on a collision with a **client-wired** action name. This needs the new small `internal/parse/` wired-name pass, landed in **Phase 1**, gated by **`V19`**. Verify the warning is not silently dropped.
- **Bounded (not severe) — `Publish` is send-side ungated; gate at the caller.** Neither `ctx.Publish` nor `handler.Publish` runs the ACL; the Subscribe-time ACL gates *who reads*. There is **no built-in all-users topic** (`GlobalTopic()` was removed — Appendix B), so no topic reaches the whole user base by construction. The residue is bounded to one identity (`UserTopic`) or exactly the connections the app's own ACL admitted. Design intent: an app-wide announcement is an ordinary developer topic the app must allow in `WithTopicACL` and publish from trusted code. Docs (Phase 6) must surface this as named guidance, not a buried paragraph.
- **Intentional, not new — fan-out backpressure is drop-on-overflow.** `Publish` local fan-out enqueues via `Connection.EnqueueDispatch` (non-blocking, drops on a full per-connection buffer). Identical to existing `BroadcastAction` behavior (not a regression); surfaced by `wsBufferFull` / `wsSlowClientCloses`; tuned via `WithWebSocketBufferSize` / `LVT_WS_BUFFER_SIZE`. Treat as the accepted pre-existing model, not a new problem to solve in this work.
- **Explicitly rejected in Appendix B — do not reintroduce without a logged maintainer decision:** a built-in `GlobalTopic()`/all-users primitive; a `SessionTopic(groupID)` constructor; a `Publish` debounce/coalesce helper; a trie/radix pattern index or glob engine; `GroupID`-field reuse for the topic envelope (a new `Topic` field is used instead).
- **Multi-segment wildcards are IN v1** (Phase 3; Appendix B "single trailing `*` only" was superseded). The matcher is a flat O(P) segment scan by design. The open contingency is whether multi-segment **holds** under implementation pressure — Phase 3's Learnings makes the explicit call; if it is reduced, log the decision + rationale in the journal below (Appendix B stays frozen — the journal entry is the record).

### Surfaced during Phase N (fill in as discovered)

- _Phase 0:_ (1) **⚠ Latent subscribe-after-`Unregister` leak** — `SubscribeConnectionToTopic` has no connection-liveness guard (none needed in Phase 0: no caller). Phase 1's `ctx.Subscribe` exposes it; Phase 1 **must** choose policy (a) registry `<-conn.done` short-circuit (matches `EnqueueDispatch`) or (b) `ctx.Subscribe` error return. See `phase-0.md`. (2) **`GOWORK=off` invariant** for manual go cmds in the nested worktree (pre-commit hook self-handles it — verified `scripts/pre-commit.sh:18,35,154`). (3) **`GetByTopicExcept` gained an injected `match` param** (forced by the `internal/session`→root import-cycle boundary; Phase 1 `dispatchToTopic` passes `segmentMatch`). (4) **Pre-existing `-race` flake `TestRangeBuildLatency_PostPhase7`** — hard wall-clock ceilings, no `-race`/`-short` guard; reproduced identically on clean `main`; unrelated to Phase 0; gate is non-race so unaffected; later phases must scope `-race` to relevant pkgs, not full `./...`. None map onto an Appendix-B alternative (multi-segment wildcards remain IN v1 as the spec intends).
- _Phase 1:_ (1) **nil-`r` ACL hardening (shipped).** Server-originated Contexts (dispatched/server-initiated/upload) inject `r=nil`; the spec's own canonical ACL pattern derefs `r`, so `checkTopicACL` now deny-by-defaults when `r==nil` before calling the hook (SelfTopic ACL-exempt, never reaches it). Phase 4/6 docs: hooks aren't called from server-originated contexts. (2) **WS error envelope is WS-connect-path only**, server-side, Phase 1; keep-open-vs-close (V14) finalization deferred to Phase 4 — envelope contract `{"type":"error","code":"topic_forbidden","topic":…}`. (3) **HTTP-GET ACL-denial → HTTP 500** (no 403 remap; spec doesn't require it) — Phase 6 troubleshooting-docs note. (4) **`ctx.Publish` permissive on `lvt:`** (no SelfTopic-equality; anti-spoof is Subscribe-side §2, send ungated §3) — pinned, record for Phase 4/6. (5) **`lvt-on:` client-side only** → wired-name pass is a statics regex scan, not `internal/parse/` nodes (Phase 6 docs). (6) **`ServerActionMessage` has no `GroupID`** → used per-conn `conn.GroupID` (bot-verifiable correction). (7) **subscribe-after-`Unregister` race = silent-drop** in `SubscribeConnectionToTopic` (`<-conn.done`, mirrors `EnqueueDispatch`; user-confirmed). (8) **PR #419 Claude-bot round (`28b8f6cb`):** fixed 2 functional bugs (upload-complete Publish drain; Mount/OnConnect Publish now fans out per spec §"Publish on GET-phase Mount is not a no-op"), test-race lock, app-global collision-warn dedup, 2 doc caveats; **declined** the pre-existing `processBroadcasts`-on-upload gap (out of scope, `BroadcastAction` untouched until Phase 5); **deferred** a 6-item test-coverage matrix to Phase 2 (bot-framed "not blockers"; see `phase-1.md`). (9) **Round 2/3 converged (no new functional issues):** doc-clarity line, conditional `wiredCollisionWarned` alloc, comment-density trims, 3 cosmetic doc-nits — all addressed; `wsAction`-dup declined (no dup exists). (10) **⚠ Pre-existing flaky `pubsub` `-race`** (`TestReconnect_DoesNotBlockConcurrentPublish`) surfaced on PR #419 cross-repo `-race ./...`; not Phase 1's (0 pubsub files changed; green 3× locally); re-ran the job; **Phase 2 owns the fix** (see Phase 2 row + `phase-1.md`). None map onto an Appendix-B alternative; multi-segment wildcards remain IN v1 (Phase 3).
- _Phase 2:_ (1) **Registry public-surface change (not an Appendix-B item; a correctness requirement).** `SubscribeConnectionToTopic`/`UnsubscribeConnectionFromTopic` now return the per-connection 0→1/1→0 transition `bool`; new `SubscribedTopics(conn) []string` snapshot accessor. Needed for exactly-once cross-instance relay (broadcaster channel refcount is instance-wide; registry subscribe is per-connection idempotent). Phase 3's pattern relay reuses the identical transition-bool + LIFO disconnect-sweep shape. (2) **Disconnect relay teardown is root-package-driven** (`releaseRelayedTopics` deferred after `Unregister` so LIFO runs it while `conn.subscribedTopics` is live) — `internal/session` can't import `pubsub` (cycle), so it can't live in `Unregister`. (3) **Seen-ring dedup deferred to Phase 3** (advisor-confirmed): Phase 2 shipped only the `Topic`+`Seq` wire plumbing + per-instance atomic counter (incremented on every `GroupActionMessage` emit, group-action AND topic, per spec "Seq scope"); the ring is the consumer of the SUBSCRIBE+PSUBSCRIBE double-fire that exists only in Phase 3 (V17). `Seq` round-trip cross-instance-proven by V8/V9. (4) **Phase 1 round-4 normalization fix landed** (was deferred to Phase 2): `canonicalActionKey = toSnakeCase` (the form `methodNameToActions` reduces every style to); closes the "save" vs "Save" false-negative; V19 green (logged value stays the raw action). (5) **Benign pre-existing testcontainer-cleanup log** (`redis.go:146 "client is closed"`) also fires in pre-existing `TestRedisBroadcaster_RefcountSurvivesUnsubscribeUntilZero`; `getTestRedisClient`'s own cleanup vs broadcaster Close ordering — harmless (container reaped regardless). (6) **`LiveHandler.Publish` ≠ `*Template.Publish`** — the spec's "handler" is the value `Template.Handle()` returns. None map onto an Appendix-B alternative; multi-segment wildcards remain IN v1 (Phase 3).
- _Phase 3:_ (1) **`Publish` to a `*` pattern panicked `GetByTopicExcept`** ("concrete must not contain \"*\""). Latent in Phases 0–2 (only concretes published); Phase 3 makes `room/*` a first-class Subscribe target so a mistaken `Publish("room/*",…)` became a plausible server-crash footgun. **Fixed in-phase**: `isPatternTopic` reject guard at both `ctx.Publish` and `handler.Publish` (clean error; "patterns are Subscribe-only"). Correctness/ergonomics requirement, not an Appendix-B item. (2) **`subKind` is a `pubsub`-internal surface change**: unexported `subscribeTo`/`trySubscribe`/`unsubscribeFrom` gained a `subKind` param (single-sources the #420/#422 race loop across SUBSCRIBE/PSUBSCRIBE); 5 in-package `redis_test.go` callers updated to `subExact`. No exported API change; group-action wire/semantics byte-unchanged. (3) **Pattern-path race coverage = the single-sourced seam** (advisor-concurred): `TestSubscribeTo_DoesNotBlockConcurrentOperations` already exercises the kind-aware `trySubscribe` against the same `currentSubscribeHook`; `pubsub -race` (25.9s) covers the pattern path — no dedicated concurrency test added (recorded decision, not deferred). (4) **Seen-ring placement**: pubsub-layer `handleTopicActionMessage` (the canonical §"Cross-instance exactly-once" "inside the processMessages pump" reading), recorded as Deviation 1 — not a user decision. None map onto an Appendix-B alternative. **Multi-segment wildcards remain IN v1 and HOLD** — the explicit Phase-3 call (logged in Decisions below).
- _Phase 4:_ (1) **Keep-open requires a livetemplate server change** (the central Audit reshape). Phase 4 brought the keep-open finalization Phase 1 deferred into a `mount.go` change (Option B): on `*TopicForbiddenError`, send envelope + Warn + fall through to the shared success-path lifecycle (adopt newState, persistState, OnConnect, drain pending publishes, send initial tree); non-TopicForbidden Mount errors keep byte-identical close behavior. Not an Appendix-B alternative; the proposal-pinned V14 "WS stays open" requirement is structurally unsatisfiable on the Mount path without it (chain: server emits envelope only on TopicForbidden → previously also closed → client `transport/websocket.ts:49` auto-reconnects → re-Mount → re-deny → storm). (2) **`lvt:error` name overlaps with `state/form-lifecycle-manager.ts`'s form-level event** — different target (wrapper vs `<form>`), disjoint detail shape (`{code,topic}` vs `ResponseMetadata`), both non-bubbling. No functional collision (capture phase observes wrapper dispatch regardless of `bubbles:false`; both events ignore the other's target). Pinned per spec (advisor call: spec-pinned name, ship per spec). **Phase 6 docs must distinguish.** (3) **Cross-repo `replace` is the Phase-5-resolved artifact pattern.** lvt go.mod gained a committed `replace github.com/livetemplate/livetemplate => ../../../livetemplate/.worktrees/broadcast-redesign-phase-4` with an inline comment naming Phase 5 as its resolution — the lvt Phase-4 branch is intentionally not independently CI-runnable / mergeable, matching the proposal's "lvt e2e gates last (consumes both)" release order. Canonical shape for future cross-repo dev work introducing an API on the dependee before the dependency publishes. (4) **Pre-existing lvt `scripts/pre-commit.sh` lint-flag bug** — `golangci-lint run --disable-all` flag was removed in v2; lvt main line 36 still has it, so the lint step silently fails at flag-parse and the lvt lint gate has been effectively non-enforced (84+ pre-existing issues accumulated). Confirmed pre-existing on lvt main; not introduced by Phase 4. Verified my added file is lint-clean with correct v2 flags (`--default=none --enable=…`). **Out of Phase 4 scope; one-line fix flagged for Phase 5/6.** (5) **lvt CDN client + 1h disk cache shadows `LVT_CLIENT_CDN_URL`** — the canonical `e2etest.ServeClientLibrary` cannot reliably serve unreleased client changes. For Phase 4 the V14 e2e mounts a local-bundle handler reading the worktree-built `dist/livetemplate-client.browser.js`; Phase 5/6 swaps back to `ServeClientLibrary` after `@livetemplate/client` publishes. (6) **Mount runs on every HTTP request AND WS connect** (livetemplate `CLAUDE.md`). A controller subscribing a topic the ACL may deny must guard with `IsInitialMount()` (or equivalent) to skip the Subscribe on the HTTP-GET render — otherwise the page 500s before the WS can exercise V14's keep-open. The V14 e2e controller uses this guard (Deviation 3); the first failed e2e run surfaced this via the 4-artifact dump. **Phase 6 docs deliverable.** (7) **Controller-swallows-error case: no envelope emitted.** `_ = ctx.Subscribe("denied"); return s, nil` → Mount returns nil → no envelope path → no `lvt:error`. V14's contract is on the propagated-error scenario. Acceptable per spec but **Phase 6 docs must surface** to avoid overpromising. (8) **HTTP-GET denial behavior remains HTTP 500** (Phase 1 recorded; Phase 4 only changes the WS-connect path's close→keep-open). Pin in Phase 6 troubleshooting docs. None map onto an Appendix-B alternative; multi-segment wildcards remain IN v1 (Phase 3, unrelated to Phase 4's surface).
- _Phase 5:_ (1) **`PublishGroupAction` kept, audit was wrong.** `session_impl.go:180` (`localSession.TriggerAction`) calls `PublishGroupAction` directly for the cross-instance fan-out of server-initiated actions, independent of the removed `BroadcastAction`. The `pubsub.GroupActionBroadcaster` interface stays; docs reworded to frame the method as `Session.TriggerAction`-backed. Phase 6 V20 must NOT flag these as drift — they are live API surface. (2) **`MaxBroadcastsPerAction` renamed, not deleted.** Phase 4's Publish reuses the same cap (`topic_context.go:177`); deletion would have silently uncapped Publish. Renamed `MaxPublishesPerAction`, relocated to `topic_context.go` next to the use site. Exported constant rename is itself a v0.10.0 breaking-change line — document in release notes alongside the BroadcastAction removal. (3) **`topic_phase2_coverage_test.go` was missed by the audit** — 6 references including the cap-overflow loop and error-message assertion all updated. (4) **Word-boundary regex blind spot (process learning).** `\bBroadcastAction\b` does NOT match `TestBroadcastAction_PeerCanSetFlash` because `_` is `\w`. Phase 6 V20 sweep MUST use the substring form `grep 'BroadcastAction\|...'`, not the word-boundary form. None map onto an Appendix-B alternative.
- _Phase 6:_ TBD — final reconciliation logged here (Appendix B stays frozen; this is the record).

---

## Decisions Reaffirmed / Reversed

The proposal body + Appendix B "Alternatives considered" is the immutable baseline. If a phase must reverse or add to a decision (e.g., reintroduce a rejected alternative, or reduce multi-segment wildcards), log it here with date and one-sentence reason; the proposal stays the original baseline, this is the journal.

| Date | Decision reversed / added | New choice | Reason |
|---|---|---|---|
| 2026-05-19 | Phase 3 explicit call: do multi-segment wildcards HOLD in v1? (proposal Phase-3 Learnings requirement) | **REAFFIRMED — HOLD, no reduction.** Multi-segment (`room/*/log`, `*/alice`, `a/*/b/*`) ships in v1; matcher stays the flat O(P) `segmentMatch` scan (no trie/glob). | No implementation pressure to reduce: the general-case `segmentMatch` Phase 0 built carried straight through to cross-instance PSUBSCRIBE unchanged (Redis `*`-spans-`/` over-delivery is dropped for free by the same equal-segment-count rule). Bench item 2 confirms O(P) ≈220ns/pattern is adequate at expected counts — no index needed. Appendix B ("single trailing `*` only" — already superseded by the spec body) stays frozen; this journal entry is the record. |

---

## Per-Phase Learnings File Template

Create each `phase-N.md` from this skeleton (kept identical to the §"Per-phase audit + learnings protocol" copy in the proposal — if you change one, change both):

<!-- INVARIANT: keep this skeleton byte-identical to the copy in the proposal §"Per-phase audit + learnings protocol" — change both together -->
```markdown
# Phase N — Learnings

**Session:** <date / id>   **Status at exit:** complete | partial | blocked
**Plan reference:** Phase N in docs/proposals/broadcast-action-redesign-proposal.md

## What shipped
## Deviations from plan
## New scope surfaced (rolled into the Surfaced-Scope & Deferral Ledger)
## Adjustments recommended for the next phase
## Open questions for the user
## File / commit / V-item pointers
```
