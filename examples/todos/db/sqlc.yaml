version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "."
        emit_json_tags: true
        emit_interface: false
        emit_exact_table_names: false
