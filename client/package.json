{
  "name": "@livefir/livetemplate-client",
  "version": "0.1.1",
  "description": "TypeScript client for LiveTemplate tree-based updates",
  "main": "dist/livetemplate-client.js",
  "browser": "dist/livetemplate-client.browser.js",
  "types": "dist/livetemplate-client.d.ts",
  "scripts": {
    "build": "tsc && npm run build:browser",
    "build:browser": "esbuild livetemplate-client.ts --bundle --format=iife --global-name=LiveTemplateClient --outfile=dist/livetemplate-client.browser.js --sourcemap",
    "test": "jest --no-cache",
    "test:reconstruction": "jest test-reconstruction",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "livetemplate",
    "html",
    "tree-updates",
    "phoenix-liveview",
    "htmx",
    "websocket",
    "client"
  ],
  "author": "LiveFir",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/livefir/livetemplate.git",
    "directory": "client"
  },
  "homepage": "https://github.com/livefir/livetemplate#readme",
  "bugs": {
    "url": "https://github.com/livefir/livetemplate/issues"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.10",
    "jest": "^30.1.3",
    "jest-environment-jsdom": "^30.2.0",
    "ts-jest": "^29.4.4",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/**/*",
    "README.md",
    "LICENSE"
  ],
  "dependencies": {
    "@types/morphdom": "^2.3.0",
    "morphdom": "^2.7.7"
  }
}
